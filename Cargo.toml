[package]
name = "dust"
version = "0.1.0"
authors = ["Asger Nyman Christiansen <asgernyman@gmail.com>"]

[dependencies]
gust = { path = "../Gust" }
gl = { path = "lib/gl" }

[features]
gl_debug = ["gl/debug"]
